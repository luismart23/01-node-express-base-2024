# Proyecto con node.js

## Primeros pasos

Crear archivo:
```sh
index.js
```

Inicializar package.json:
```sh
npm init -y
```

Instalar express:
```sh
npm i express
```

No subir jamas la carpeta node modules:
```sh
.gitignore
```